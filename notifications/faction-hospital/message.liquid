{
	"embeds": [
		{
			"title": "Hospitalized Members of {{ faction_name }}",
			TODO: Handle when no one is in the hospital
			"description": "{% for index in (1..hospitalized_members.size) limit:10 %}[https://torn.com/profiles.php?XID={{ hospitalized_members[index].id }}]({{ hospitalized_members[index].username }}) - <t:{{ hospitalized_members[index].timestamp }}:R>\n{% endfor %}"
		}
	]
}
